char* StrToUpper(char* str) {
        char* p;
        for (p = str ; *p ; ++p) *p = toupper(*p);
        return str;
		//   REALLY LONG LINE.....................................................................................
    }